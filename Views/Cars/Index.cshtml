@model List<RentACarService.Models.Car>

@{
    ViewData["Title"] = "Available Cars";
}

<section class="page-section">
    <div class="container">
        <div class="page-title-row">
            <div>
                <span class="section-label">Choose your vehicle</span>
                <h1>Available Cars</h1>
            </div>
        </div>

        <div class="filter-box mb-4">
            <form>
                <div class="row g-3 align-items-end">
                    <div class="col-md-6 col-lg-3">
                        <label class="form-label" for="brand">Brand</label>
                        <input class="form-control" id="brand" type="text" placeholder="Toyota" />
                    </div>
                    <div class="col-md-6 col-lg-3">
                        <label class="form-label" for="type">Car type</label>
                        <select class="form-select" id="type">
                            <option selected>All types</option>
                            <option>Sedan</option>
                            <option>SUV</option>
                            <option>Hatchback</option>
                            <option>Van</option>
                        </select>
                    </div>
                    <div class="col-md-6 col-lg-3">
                        <label class="form-label" for="transmission">Transmission</label>
                        <select class="form-select" id="transmission">
                            <option selected>Any</option>
                            <option>Automatic</option>
                            <option>Manual</option>
                        </select>
                    </div>
                    <div class="col-md-6 col-lg-3">
                        <label class="form-label" for="priceRange">Price range</label>
                        <select class="form-select" id="priceRange">
                            <option selected>Any price</option>
                            <option>$0 - $50</option>
                            <option>$50 - $80</option>
                            <option>$80+</option>
                        </select>
                    </div>
                </div>
            </form>
        </div>

        <div class="row g-4">
            @foreach (var car in Model)
            {
                <div class="col-md-6 col-xl-4">
                    <div class="car-card card h-100">
                        <div class="car-image-placeholder">Car Image</div>
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-start gap-3 mb-2">
                                <div>
                                    <h5 class="card-title mb-1">@car.Brand @car.Model</h5>
                                    <p class="text-muted mb-0">@car.Type</p>
                                </div>
                                <span class="badge @(car.IsAvailable ? "text-bg-success" : "text-bg-secondary")">
                                    @(car.IsAvailable ? "Available" : "Unavailable")
                                </span>
                            </div>
                            <div class="spec-list">
                                <span>@car.Year</span>
                                <span>@car.FuelType</span>
                                <span>@car.Transmission</span>
                            </div>
                            <div class="d-flex justify-content-between align-items-center mt-3">
                                <span class="price-text">$@car.DailyPrice/day</span>
                                <a class="btn btn-outline-primary btn-sm" asp-controller="Cars" asp-action="Details" asp-route-id="@car.Id">View Details</a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</section>
